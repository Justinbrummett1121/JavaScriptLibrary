/* 
literals are the values we as devs initialize the variables with.
strings numeric boolean and array

*/

//string literal
let car = "Ford";

//numeric literal the number we supplied to our code
let december = 12;

//boolean literal
let tired = false;

//object literal
let soup = {
    a: "chicken Noodle",
    b: "tomato",
    c: "beeg and barley"
}

console.log(soup.a);

//array literal
let days = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday"]

console.log(days.toString());
console.log(days.length);